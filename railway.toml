[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'npx prisma db push --skip-generate && npx tsx prisma/seed.ts; HOSTNAME=0.0.0.0 node .next/standalone/server.js'"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
