[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'npx prisma migrate deploy; HOSTNAME=0.0.0.0 node .next/standalone/server.js'"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
